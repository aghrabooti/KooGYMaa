# -*- coding: utf-8 -*-
"""فصل سوم – طراحی، پیاده‌سازی و راهنمای استفاده"""
from build import fa, en, code, p, ul, ol, h2, h3, h4, chapter, end_chapter, fig, two_up, mobile_pair, table, ref, fn

def render(N):
    o = []
    o.append(chapter(3, "طراحی، پیاده‌سازی و راهنمای استفادهٔ سامانهٔ کوجیما"))
    o.append(p("این فصل بدنهٔ اصلی پایان‌نامه است و در دو بخش ارائه می‌شود. بخش نخست (۳-۱ تا ۳-۹) جنبه‌های فنی را پوشش می‌دهد: معماری کلی، مدل داده، جریان‌های اصلی سامانه با فلوچارت و نمودار توالی، امنیت، بومی‌سازی، طراحی واکنش‌گرا، اتوماسیون و آزمون. بخش دوم (۳-۱۰ تا ۳-۱۳) راهنمای استفادهٔ قدم‌به‌قدم برای سه نقش مدیر باشگاه، مربی و عضو است که با تصاویر واقعی از سامانه همراه شده است. برای درک بخش نخست، آشنایی با مفاهیم فصل دوم کافی است.", "first"))

    # ------------------------------------------------------------------ 3-1
    o.append(h2("3-1", "معماری کلی سامانه"))
    o.append(p("کوجیما یک برنامهٔ وب یکپارچه " + fn(1) + " است که رابط کاربری، منطق دامنه و API را در یک پروژهٔ Next.js نگه می‌دارد. شکل " + fa(3) + "-" + fa(1) + " بلوک‌دیاگرام کلی سامانه را در چهار لایه نشان می‌دهد: مرورگر، چارچوب Next.js (صفحات سرور و مسیرهای API)، لایهٔ دامنه و دادهٔ ماندگار. جریان یک درخواست معمولی چنین است: مرورگر صفحه‌ای را درخواست می‌کند؛ مؤلفهٔ سرور مربوط پس از خواندن نشست کاربر، مستقیماً از طریق Prisma داده را می‌خواند و HTML آماده را بازمی‌گرداند. عملیات نوشتنی از طریق جزیره‌های کلاینت به مسیرهای API ارسال می‌شود که پس از احراز هویت، محدودیت نرخ و اعتبارسنجی، توابع لایهٔ دامنه را فرامی‌خوانند.", "first"))
    o.append('<div class="footnotes"></div><div class="fnline"><sup>1</sup> Monolith</div>')
    o.append(fig("diag/arch.png", "بلوک‌دیاگرام کلی سامانهٔ کوجیما در سطح لایه‌ها", cls="w90"))
    o.append(p("این معماری در برابر معماری ریزسرویس " + fn(1) + " انتخاب شد، زیرا برای تیمی کوچک و مقیاس هدف (ده‌ها باشگاه و هزاران کاربر) هزینهٔ عملیاتی بسیار کمتری دارد و همچنان با جداسازی منطق دامنه در پوشهٔ " + code("lib/") + "، در صورت نیاز آینده قابل تفکیک است. شکل " + fa(3) + "-" + fa(2) + " ارتباط سه نقش سامانه با یکدیگر و با موجودیت باشگاه را نشان می‌دهد."))
    o.append('<div class="footnotes"></div><div class="fnline"><sup>1</sup> Microservices</div>')
    o.append(fig("diag/roles.png", "نقش‌های سامانه و تعامل آن‌ها حول موجودیت باشگاه", cls="w70"))
    o.append(h3("3-1-1", "ساختار پروژه"))
    o.append(p("ساختار پوشه‌های پروژه از قراردادهای App Router پیروی می‌کند و در جدول " + fa(3) + "-" + fa(1) + " خلاصه شده است. نکتهٔ کلیدی این ساختار، تفکیک صریح سه لایه است: " + code("app/") + " فقط مسیریابی و ترکیب صفحه را بر عهده دارد، " + code("components/") + " واحدهای رابط کاربری قابل استفادهٔ مجدد را نگه می‌دارد و " + code("lib/") + " منطق دامنه را بدون هیچ وابستگی به React پیاده می‌کند تا مستقلاً قابل آزمون باشد.", "first"))
    o.append(table("ساختار پوشه‌های اصلی پروژه", ["مسیر", "محتوا", "تعداد فایل"], [
        [code("app/"), "صفحات (page.tsx)، چیدمان‌ها (layout.tsx)، مسیرهای API (route.ts) به تفکیک نقش: admin، trainer، user، auth، cron، webhooks", "۴۷ صفحه، ۷۶ مسیر API"],
        [code("components/"), "مؤلفه‌های رابط کاربری: ناوبری هر نقش، فرم‌ها، ویرایشگرهای برنامه، تقویم جلسات، نمودار پیشرفت، منوی موبایل، سوییچ زبان و تم", "۵۰+"],
        [code("lib/"), "منطق دامنه: احراز هویت و نشست، اعتبارسنجی‌ها، مالی، زمان‌بندی، اشتراک، استریک، تقویم جلالی، i18n، پرداخت، محدودیت نرخ، پایش", "۴۰+"],
        [code("prisma/"), "طرحواره (۳۳ مدل، ۱۶ شمارش)، ۶ مهاجرت SQL، اسکریپت دادهٔ اولیه", "–"],
        [code("tests/"), "آزمون‌های واحد Vitest", "۱۸ فایل، ۷۶ آزمون"],
        [code("docs/"), "مستندات پایگاه داده، پرداخت و عملیات", "۳"],
        [code("public/fonts/"), "فونت خودمیزبان Estedad (OFL)", "–"],
    ], widths=["18%", "62%", "20%"]))

    # ------------------------------------------------------------------ 3-2
    o.append(h2("3-2", "مدل داده"))
    o.append(p("طرحوارهٔ پایگاه داده ۳۳ مدل و ۱۶ نوع شمارشی دارد و در پنج خوشهٔ مفهومی سازمان یافته است: هویت و عضویت، اشتراک و پرداخت، برنامه‌های تمرینی و غذایی، اجرا و پیشرفت، و ارتباطات و حسابرسی. شکل " + fa(3) + "-" + fa(3) + " نمای کلی روابط میان همهٔ مدل‌ها را نشان می‌دهد و شکل‌های " + fa(3) + "-" + fa(4) + " و " + fa(3) + "-" + fa(5) + " دو خوشهٔ اصلی را با جزئیات فیلدها ترسیم می‌کنند.", "first"))
    o.append(fig("diag/erd.png", "نمای کلی روابط میان مدل‌های پایگاه داده (نمودار موجودیت–رابطه)", style="max-width:100%"))
    o.append(h3("3-2-1", "هویت، عضویت و اشتراک"))
    o.append(p("مدل " + en("User") + " هستهٔ سامانه است و با فیلد " + code("role") + " یکی از سه نقش را می‌گیرد. رابطهٔ کاربر با باشگاه بسته به نقش از سه جدول متفاوت می‌گذرد: " + en("GymStaff") + " برای مدیران، " + en("GymTrainer") + " برای مربیان و " + en("GymMembership") + " برای اعضا. این تفکیک به مربی اجازه می‌دهد هم‌زمان با چند باشگاه همکاری کند و به عضو اجازه می‌دهد در چند باشگاه عضو باشد. رابطهٔ مستقیم مربی و شاگرد – مستقل از باشگاه – در " + en("TrainerClient") + " نگهداری می‌شود که یکی از تصمیم‌های کلیدی طراحی برای رفع «محدودیت جغرافیایی» بیان‌شده در فصل اول است.", "first"))
    o.append(p("هر باشگاه چند " + en("SubscriptionPlan") + " با قیمت (به کوچک‌ترین واحد پول، مثلاً ریال)، ارز، مدت و مخاطب (عضو یا مربی) تعریف می‌کند. خرید یک طرح، رکورد " + en("Subscription") + " با وضعیت PENDING و یک " + en("Payment") + " می‌سازد. قیمت پرداخت‌شده در خود اشتراک کپی می‌شود تا تغییر بعدی قیمت طرح، سابقهٔ مالی را خراب نکند. رخدادهای درگاه در " + en("PaymentEvent") + " ثبت می‌شوند و کلید خودتوانی یکتا از پرداخت تکراری جلوگیری می‌کند."))
    o.append(h3("3-2-2", "برنامه‌های تمرینی و غذایی"))
    o.append(p("برنامهٔ تمرینی ساختاری سه‌سطحی دارد: " + en("WorkoutPlan") + " ← " + en("WorkoutDay") + " ← " + en("WorkoutExercise") + ". هر حرکت نام، تعداد ست، الگوی تکرار (مثلاً «۸-۱۲») و زمان استراحت دارد. برنامه می‌تواند «الگو» باشد و با عمل کپی برای شاگرد خاص شخصی‌سازی شود؛ فیلد " + code("version") + " امکان نگهداری نسخه‌های متوالی را می‌دهد. تخصیص برنامه به شاگرد از طریق " + en("WorkoutAssignment") + " با تاریخ شروع و وضعیت انجام می‌شود. برنامهٔ غذایی ساختار مشابهی دارد: " + en("DietPlan") + " ← " + en("DietDay") + " ← " + en("Meal") + " ← " + en("FoodItem") + " با هدف کالری. شکل " + fa(3) + "-" + fa(5) + " این خوشه را همراه با جدول‌های ثبت اجرا نشان می‌دهد.", "first"))
    o.append(fig("diag/erd-core.png", "خوشهٔ هویت، عضویت، اشتراک و پرداخت با فیلدهای اصلی", cls="tall"))
    o.append(h3("3-2-3", "اجرا، پیشرفت و ارتباطات"))
    o.append(p("وقتی عضو تمرینی را انجام می‌دهد، یک " + en("WorkoutLog") + " با وضعیت، شدت درک‌شده و زمان اجرا و به‌ازای هر حرکت یک " + en("ExerciseLog") + " (ست، تکرار و وزنهٔ واقعی) ثبت می‌شود. ثبت تغذیه با " + en("NutritionLog") + "/" + en("MealLog") + "، اندازه‌های بدنی با " + en("BodyMeasurement") + " و عکس‌ها با " + en("ProgressPhoto") + " نگهداری می‌شوند. همهٔ این جدول‌ها کلید خارجی به " + en("User") + " دارند نه به باشگاه – تجلی اصل «مالکیت سابقه توسط فرد». ارتباطات شامل " + en("Notification") + " (پنج نوع)، " + en("Feedback") + " (گفت‌وگوی مربی و شاگرد روی یک لاگ)، " + en("GymReview") + " و " + en("TrainerReview") + " است و " + en("AuditLog") + " هر عمل اداری را با کنشگر، موجودیت و جزئیات ثبت می‌کند.", "first"))
    o.append(h3("3-2-4", "انواع شمارشی و وضعیت‌ها"))
    o.append(p("وضعیت‌های چرخهٔ عمر موجودیت‌ها به‌صورت نوع شمارشی در طرحواره تعریف شده‌اند تا مقادیر نامعتبر در سطح پایگاه داده رد شوند. جدول " + fa(3) + "-" + fa(2) + " مهم‌ترین آن‌ها را فهرست می‌کند.", "first"))
    o.append(fig("diag/erd-training.png", "خوشهٔ برنامه‌های تمرینی و غذایی و ثبت اجرای آن‌ها", cls="tall"))
    o.append(table("انواع شمارشی اصلی طرحواره", ["شمارش", "مقادیر", "کاربرد"], [
        [en("Role"), en("ADMIN, TRAINER, USER"), "نقش سراسری کاربر"],
        [en("MembershipStatus"), en("PENDING, APPROVED, REJECTED, SUSPENDED"), "درخواست عضویت عضو / همکاری مربی در باشگاه"],
        [en("TrainerClientStatus"), en("PENDING, ACTIVE, PAUSED, ENDED"), "رابطهٔ مربی–شاگرد"],
        [en("SubscriptionStatus"), en("PENDING, ACTIVE, PAUSED, EXPIRED, CANCELLED"), "چرخهٔ عمر اشتراک"],
        [en("PaymentStatus"), en("PENDING, PROCESSING, SUCCEEDED, FAILED, REFUNDED, CANCELLED"), "چرخهٔ عمر پرداخت"],
        [en("PaymentType"), en("SUBSCRIPTION, RENEWAL"), "تفکیک خرید اولیه از تمدید"],
        [en("PlanStatus"), en("DRAFT, PUBLISHED, ARCHIVED"), "برنامهٔ تمرینی/غذایی"],
        [en("SessionStatus"), en("SCHEDULED, COMPLETED, CANCELLED, NO_SHOW"), "جلسهٔ حضوری"],
        [en("NotificationType"), en("PLAN_ASSIGNED, FEEDBACK, SESSION_REMINDER, SUBSCRIPTION_EXPIRING, GENERAL"), "دسته‌بندی اعلان‌ها"],
    ], widths=["22%", "48%", "30%"]))

    # ------------------------------------------------------------------ 3-3
    o.append(h2("3-3", "احراز هویت و مدیریت حساب"))
    o.append(h3("3-3-1", "ثبت‌نام"))
    o.append(p("فلوچارت شکل " + fa(3) + "-" + fa(6) + " فرایند ثبت‌نام را نشان می‌دهد. اعتبارسنجی دو بار انجام می‌شود: در مرورگر برای بازخورد فوری و در سرور به‌عنوان مرجع نهایی (چون ورودی مرورگر هرگز قابل اعتماد نیست). قواعد اعتبارسنجی در " + code("lib/auth-validation.ts") + " به‌صورت توابع خالص نوشته شده و مستقلاً آزمون می‌شوند. اگر نقش انتخابی «مربی» باشد، هم‌زمان با کاربر یک " + en("TrainerProfile") + " خالی نیز ساخته می‌شود. پس از ثبت موفق، کاربر بلافاصله وارد سامانه شده و به پنل نقش خود هدایت می‌شود.", "first"))
    o.append(h3("3-3-2", "ورود و نشست"))
    o.append(p("شکل " + fa(3) + "-" + fa(7) + " فلوچارت ورود و شکل " + fa(3) + "-" + fa(8) + " نمودار توالی آن را در تعامل با لایه‌های داخلی نشان می‌دهد. سه نکتهٔ امنیتی در این جریان قابل توجه است. نخست، اگر ایمیل وارد‌شده وجود نداشته باشد، سامانه باز هم یک مقایسهٔ bcrypt با هش ساختگی انجام می‌دهد تا زمان پاسخ برای ایمیل موجود و ناموجود یکسان باشد و حمله‌کننده نتواند از اختلاف زمان، وجود حساب را تشخیص دهد " + fn(1) + ". دوم، برای هر ورود موفق یک رکورد " + en("UserSession") + " با آدرس IP و عامل کاربر ساخته و شناسهٔ آن در ادعاهای JWT قرار می‌گیرد؛ در هر درخواست بعدی، ابطال‌نشده بودن این نشست بررسی می‌شود. سوم، مدت اعتبار کوکی بسته به انتخاب «مرا به خاطر بسپار» ۲۴ ساعت یا ۷ روز است.", "first"))
    o.append('<div class="footnotes"></div><div class="fnline"><sup>1</sup> Timing attack mitigation</div>')
    o.append(fig("diag/flow-register.png", "فلوچارت فرایند ثبت‌نام", cls="tall"))
    o.append(h3("3-3-3", "بازیابی حساب"))
    o.append(p("فراموشی گذرواژه با توکن یک‌بارمصرف پیاده شده است: سرور یک توکن تصادفی می‌سازد، هش آن را همراه با زمان انقضا (۳۰ دقیقه) ذخیره می‌کند و توکن خام را برای کاربر می‌فرستد. هنگام بازنشانی، هش توکن دریافتی با مقدار ذخیره‌شده مقایسه می‌شود؛ بنابراین حتی با دسترسی به پایگاه داده نمی‌توان توکن معتبر ساخت. همین سازوکار برای تأیید ایمیل به کار رفته است. کاربر همچنین می‌تواند از داخل پنل، گذرواژه را تغییر دهد و فهرست نشست‌های فعال خود را ببیند و هر یک را (یا همه به‌جز نشست فعلی را) باطل کند.", "first"))
    o.append(fig("diag/flow-login.png", "فلوچارت فرایند ورود", cls="tall"))
    o.append(fig("diag/seq-auth.png", "نمودار توالی ورود و دسترسی به صفحهٔ محافظت‌شده", cls="w90"))

    # ------------------------------------------------------------------ 3-4
    o.append(h2("3-4", "جریان‌های اصلی کسب‌وکار"))
    o.append(h3("3-4-1", "درخواست عضویت و همکاری"))
    o.append(p("ورود عضو به باشگاه و پیوستن مربی به باشگاه هر دو با الگوی «درخواست–تأیید» انجام می‌شود (شکل " + fa(3) + "-" + fa(9) + "). درخواست با وضعیت PENDING ثبت و اعلانی برای مدیر ساخته می‌شود. مدیر می‌تواند تک‌تک یا گروهی تصمیم بگیرد؛ هر تصمیم در " + en("AuditLog") + " ثبت می‌شود و به درخواست‌دهنده اعلان می‌رود. همین الگو برای درخواست مربی‌گری از سوی عضو نیز به کار می‌رود، با این تفاوت که تصمیم‌گیرنده خودِ مربی است.", "first"))
    o.append(fig("diag/flow-request.png", "الگوی درخواست–تأیید برای عضویت، همکاری مربی و رابطهٔ مربی–شاگرد", cls="w80"))
    o.append(h3("3-4-2", "خرید اشتراک و پرداخت"))
    o.append(p("شکل " + fa(3) + "-" + fa(10) + " فلوچارت کامل خرید اشتراک را نشان می‌دهد. سرویس پرداخت (" + code("lib/payments/service.ts") + ") پشت یک واسط " + en("PaymentProvider") + " قرار دارد که فقط یک متد " + code("createCheckout") + " دارد؛ در نسخهٔ فعلی یک «درگاه نمایشی» این واسط را پیاده می‌کند که توکن امضاشده با HMAC می‌سازد و صفحهٔ پرداخت داخلی را نمایش می‌دهد. اتصال به درگاه واقعی (مثلاً زرین‌پال یا درگاه‌های بانکی) تنها به پیاده‌سازی همین واسط نیاز دارد. نتیجهٔ پرداخت از طریق مسیر " + code("/api/webhooks/payments/[provider]") + " دریافت و امضای آن با " + code("timingSafeEqual") + " بررسی می‌شود؛ سپس در یک تراکنش پایگاه داده، پرداخت SUCCEEDED و اشتراک ACTIVE می‌شود و تاریخ پایان بر اساس مدت طرح محاسبه می‌گردد.", "first"))
    o.append(p("نمودارهای حالت شکل " + fa(3) + "-" + fa(11) + " چرخهٔ عمر اشتراک و پرداخت را نشان می‌دهند. گذار «توقف موقت» به عضو اجازه می‌دهد در سفر یا بیماری، روزهای باقی‌ماندهٔ اشتراک را حفظ کند؛ هنگام ازسرگیری، تاریخ پایان به اندازهٔ مدت توقف جلو می‌رود. بازپرداخت فقط توسط مدیر و فقط برای پرداخت موفق ممکن است و در گزارش مالی از درآمد خالص کسر می‌شود."))
    o.append(fig("diag/flow-subscription.png", "فلوچارت خرید اشتراک و تأیید پرداخت", cls="tall"))
    o.append(two_up("diag/state-subscription.png", "diag/state-payment.png", "نمودار حالت اشتراک (راست) و پرداخت (چپ)", w="47%"))
    o.append(h3("3-4-3", "چرخهٔ برنامهٔ تمرینی: از طراحی تا بازخورد"))
    o.append(p("شکل " + fa(3) + "-" + fa(12) + " چرخهٔ کامل یک برنامهٔ تمرینی را نشان می‌دهد. مربی برنامه را در حالت پیش‌نویس می‌سازد، پس از انتشار آن را به یک یا چند شاگرد تخصیص می‌دهد، شاگرد اجرا و ثبت می‌کند و لاگ‌ها هم برای محاسبهٔ استریک و نمودار پیشرفت عضو و هم برای بازخورد مربی استفاده می‌شوند. ویرایشگر برنامه (" + code("workout-plan-editor.tsx") + ") یک مؤلفهٔ کلاینت است که کل ساختار روز/حرکت را در حافظه نگه می‌دارد و با یک درخواست ذخیره می‌کند؛ کتابخانهٔ حرکات (" + code("lib/exercises.ts") + ") با جست‌وجوی فارسی/انگلیسی به انتخاب سریع کمک می‌کند.", "first"))
    o.append(fig("diag/flow-plan.png", "چرخهٔ برنامهٔ تمرینی از طراحی توسط مربی تا بازخورد", style="max-width:100%"))
    o.append(h3("3-4-4", "زمان‌بندی جلسات حضوری"))
    o.append(p("مربی بازه‌های در دسترس هفتگی خود را اعلام می‌کند و عضو از میان آن‌ها جلسه رزرو می‌کند (شکل " + fa(3) + "-" + fa(13) + "). پیش از ثبت، تابع " + code("findConflicts") + " در " + code("lib/scheduling.ts") + " تداخل بازهٔ درخواستی را با جلسات موجود مربی بررسی و تابع " + code("isWithinAvailability") + " قرار گرفتن آن در بازهٔ اعلام‌شده را تأیید می‌کند. هر دو تابع خالص‌اند و با آزمون واحد پوشش داده شده‌اند. یادآوری جلسه بر اساس تنظیم شخصی هر کاربر (پیش‌فرض ۲۴ ساعت قبل) به‌صورت اعلان ارسال می‌شود.", "first"))
    o.append(fig("diag/seq-session.png", "نمودار توالی اعلام بازهٔ در دسترس و رزرو جلسه با تشخیص تداخل", cls="w90"))

    # ------------------------------------------------------------------ 3-5
    o.append(h2("3-5", "امنیت و کنترل دسترسی"))
    o.append(h3("3-5-1", "لایه‌های دفاعی"))
    o.append(p("امنیت سامانه به‌صورت چندلایه " + fn(1) + " طراحی شده و جدول " + fa(3) + "-" + fa(3) + " این لایه‌ها را خلاصه می‌کند. هر مسیر API ابتدا از " + code("requireSession") + " می‌گذرد که کوکی را می‌خواند، امضای JWT را با صادرکننده و مخاطب مشخص بررسی می‌کند و ابطال‌نشده بودن نشست را می‌سنجد. سپس توابع دسترسی مخصوص نقش (" + code("admin-access.ts") + "، " + code("trainer-access.ts") + ") مالکیت رکورد را بررسی می‌کنند؛ مثلاً " + code("requireGymAdmin(gymId)") + " تأیید می‌کند کاربر فعلی در " + en("GymStaff") + " آن باشگاه باشد.", "first"))
    o.append('<div class="footnotes"></div><div class="fnline"><sup>1</sup> Defense in depth</div>')
    o.append(table("لایه‌های امنیتی سامانه", ["لایه", "سازوکار", "محل پیاده‌سازی"], [
        ["ذخیرهٔ گذرواژه", "bcrypt با ضریب هزینهٔ ۱۲، هش ساختگی برای یکسان‌سازی زمان پاسخ", code("lib/auth.ts")],
        ["نشست", "JWT امضاشده (iss/aud)، کوکی HttpOnly + SameSite=Lax، جدول نشست قابل ابطال", code("lib/session.ts, lib/user-sessions.ts")],
        ["محدودیت نرخ", "پنجرهٔ ثابت؛ ۲۰/دقیقه برای احراز هویت، ۱۲۰/دقیقه برای API عمومی", code("lib/rate-limit.ts")],
        ["اعتبارسنجی ورودی", "توابع خالص برای هر دامنه؛ پیام خطای فارسی", code("lib/*-validation.ts")],
        ["کنترل دسترسی", "نقش سراسری + بررسی مالکیت رکورد (باشگاه، شاگرد، لاگ)", code("lib/admin-access.ts, trainer-access.ts")],
        ["پرداخت", "کلید خودتوانی یکتا، امضای HMAC-SHA256 با مقایسهٔ زمان‌ثابت", code("lib/payments/")],
        ["حسابرسی", "ثبت کنشگر، موجودیت، عمل و جزئیات برای هر عمل اداری", code("lib/audit.ts")],
        ["آپلود فایل", "بررسی نوع MIME و اندازه، نام‌گذاری تصادفی، خارج از ریشهٔ وب", code("progress-photos/upload")],
        ["پایش", "بافر حلقوی خطاهای سرور، شاخص‌های سلامت", code("lib/monitoring.ts, /api/health")],
    ], widths=["18%", "52%", "30%"]))
    o.append(h3("3-5-2", "ابطال نشست"))
    o.append(p("چنان‌که در فصل دوم گفته شد، JWT به‌خودی‌خود قابل ابطال نیست. راه‌حل کوجیما، قرار دادن شناسهٔ نشست در توکن و نگهداری جدول " + en("UserSession") + " با فیلد " + code("revokedAt") + " است. هزینهٔ این کار یک پرس‌وجوی سبک به‌ازای هر درخواست است که با شاخص روی کلید اصلی قابل چشم‌پوشی است. در مقابل، کاربر می‌تواند از صفحهٔ پروفایل، دستگاه‌های وارد‌شده را ببیند و «خروج از سایر دستگاه‌ها» را بزند – قابلیتی که برای حساب‌های مدیر باشگاه اهمیت ویژه دارد.", "first"))

    # ------------------------------------------------------------------ 3-6
    o.append(h2("3-6", "بومی‌سازی: فارسی، راست‌به‌چپ و تقویم جلالی"))
    o.append(p("سامانه به‌طور کامل دوزبانه است و زبان فعال در کوکی " + code("locale") + " نگهداری می‌شود (شکل " + fa(3) + "-" + fa(14) + "). در سرور، تابع " + code("getLocale()") + " این کوکی را می‌خواند و " + code("createT(locale)") + " تابع ترجمه را از دیکشنری " + code("translations.ts") + " (بیش از ۵۰۰ کلید برای هر زبان) می‌سازد؛ در کلاینت، همین دیکشنری از طریق " + en("Context") + " در دسترس است. عنصر " + code("html") + " ویژگی‌های " + code("lang") + " و " + code("dir") + " و کلاس " + code("locale-fa") + " را می‌گیرد و بقیهٔ کار را CSS انجام می‌دهد.", "first"))
    o.append(fig("diag/i18n.png", "جریان بومی‌سازی از کوکی تا رندر نهایی", cls="w90"))
    o.append(h3("3-6-1", "چیدمان راست‌به‌چپ"))
    o.append(p("به‌جای نگهداری دو برگهٔ سبک، از ویژگی‌های منطقی CSS استفاده شده است؛ در نتیجه نوار کناری پنل‌ها در فارسی به‌طور خودکار در سمت راست و در انگلیسی در سمت چپ قرار می‌گیرد. تنها استثناها عناصر تزئینی هستند (مانند نشان کارت صفحهٔ اصلی) که با موقعیت فیزیکی ثابت طراحی شده‌اند. آیکن‌های جهت‌دار (فلش‌ها) در حالت فارسی با " + code("scaleX(-1)") + " آینه می‌شوند.", "first"))
    o.append(h3("3-6-2", "تاریخ، اعداد و پول"))
    o.append(p("ماژول " + code("lib/fa.ts") + " سه دسته تابع دارد: تبدیل میلادی به جلالی (" + code("toJalali") + ") با الگوریتم مستقل و قالب‌بندی با " + code("Intl.DateTimeFormat('fa-IR-u-ca-persian')") + " در منطقهٔ زمانی تهران؛ تبدیل ارقام لاتین به فارسی (" + code("toFaDigits") + ") برای متن‌های ترکیبی؛ و قالب‌بندی پول (" + code("formatMoney") + ") که مبلغ را از کوچک‌ترین واحد به ریال/تومان با جداکنندهٔ هزارگان فارسی تبدیل می‌کند. برچسب‌های وضعیت که از انواع شمارشی پایگاه داده می‌آیند، از طریق نگاشت‌های " + code("faStatus") + " و مانند آن ترجمه می‌شوند تا هیچ مقدار انگلیسی خامی در رابط فارسی دیده نشود.", "first"))
    o.append(h3("3-6-3", "فونت"))
    o.append(p("فونت رابط کاربری فارسی، «استعداد» " + fn(1) + " با مجوز آزاد OFL است که به‌صورت متغیر (وزن ۱۰۰ تا ۹۰۰) در " + code("public/fonts/") + " میزبانی و با " + code("next/font/local") + " بارگذاری می‌شود. خودمیزبانی به این دلیل انتخاب شد که سرویس Google Fonts در بسیاری از شبکه‌های داخل کشور در دسترس نیست و وابستگی به آن باعث بازگشت بی‌صدا به فونت پیش‌فرض سیستم می‌شد. متن انگلیسی با فونت Inter نمایش داده می‌شود.", "first"))
    o.append('<div class="footnotes"></div><div class="fnline"><sup>1</sup> Estedad, by Amin Abedi (github.com/aminabedi68/Estedad)</div>')

    # ------------------------------------------------------------------ 3-7
    o.append(h2("3-7", "طراحی واکنش‌گرا و حالت روز/شب"))
    o.append(p("رابط کاربری برای عرض‌های ۳۶۰ تا ۱۲۸۰ پیکسل طراحی شده است. در عرض‌های تا ۶۴۰ پیکسل، نوار کناری پنل‌ها با یک سرصفحهٔ چسبان ۶۰ پیکسلی جایگزین می‌شود که شامل نشان برند، نام کاربر، دسترسی سریع (اعلان‌ها یا شاگردان) و دکمهٔ منوی همبرگری است؛ زیر آن یک نوار زبانه‌های افقی قابل پیمایش برای جابه‌جایی سریع قرار دارد. منوی همبرگری (" + code("components/mobile-menu.tsx") + ") یک کشوی کناری است که از سمت شروع نوشتار باز می‌شود، با کلید Escape، لمس پس‌زمینه یا تغییر مسیر بسته می‌شود، پیمایش صفحه را قفل می‌کند و ویژگی‌های " + code("aria-expanded") + " و " + code("role=dialog") + " را برای فناوری‌های کمکی دارد. شکل " + fa(3) + "-" + fa(15) + " نمای موبایل پنل عضو را با منوی بسته و باز نشان می‌دهد.", "first"))
    o.append(mobile_pair("shots/mobile-member.png", "shots/mobile-menu.png", "نمای موبایل پنل عضو (راست) و منوی همبرگری باز (چپ)"))
    o.append(p("حالت شب با کلاس " + code("theme-dark") + " روی عنصر ریشه و متغیرهای CSS برای رنگ‌های سطح، متن و خطوط پیاده شده و انتخاب کاربر در کوکی " + code("theme") + " ذخیره می‌شود تا در رندر سرور نیز اعمال شود و «پرش» رنگ هنگام بارگذاری رخ ندهد. شکل " + fa(3) + "-" + fa(16) + " داشبورد مدیر را در حالت شب نشان می‌دهد."))
    o.append(fig("shots/dark-admin.png", "داشبورد مدیر باشگاه در حالت شب", shot=True))
    o.append(p("صحت واکنش‌گرایی به‌صورت خودکار بررسی شده است: یک اسکریپت Playwright همهٔ ۳۳ صفحهٔ سامانه را در سه عرض (۳۶۰، ۷۶۸، ۱۲۸۰) و دو زبان بارگذاری و وجود سرریز افقی (" + code("scrollWidth &gt; innerWidth") + ") را می‌سنجد. در نسخهٔ نهایی، هیچ‌یک از ۱۹۸ ترکیب سرریز ندارد."))

    # ------------------------------------------------------------------ 3-8
    o.append(h2("3-8", "اتوماسیون، پایش و پشتیبان‌گیری"))
    o.append(p("عملیات تکراری با یک مسیر کرون روزانه انجام می‌شود (شکل " + fa(3) + "-" + fa(17) + "). این مسیر با سرآیند " + code("Authorization: Bearer CRON_SECRET") + " محافظت شده و سه کار انجام می‌دهد: اشتراک‌های فعالِ منقضی را – بسته به پرچم تمدید خودکار – یا تمدید می‌کند (با ثبت پرداخت از نوع RENEWAL) یا EXPIRED می‌کند؛ و برای اشتراک‌های در آستانهٔ انقضا طبق تنظیم هر کاربر یادآوری می‌فرستد. مسیر " + code("/api/health") + " تعداد اشتراک‌های عقب‌افتاده (که پس از هر اجرا باید صفر شود) و پنج خطای اخیر سرور را گزارش می‌کند تا با ابزارهای پایش بیرونی قابل هشداردهی باشد.", "first"))
    o.append(p("اسکریپت " + code("npm run db:backup") + " از فایل پایگاه داده (یا در حالت میزبانی‌شده، از محتوای جدول‌ها به‌صورت JSON) و پوشهٔ آپلودها نسخهٔ پشتیبان می‌گیرد، یک مانیفست با اندازه و چکیدهٔ هر فایل می‌نویسد و در پایان یک «آزمون بازیابی خشک» انجام می‌دهد تا از خوانا بودن نسخه اطمینان حاصل شود. شکل " + fa(3) + "-" + fa(18) + " چرخهٔ استقرار را نشان می‌دهد."))
    o.append(fig("diag/flow-cron.png", "فلوچارت زمان‌بند روزانهٔ اشتراک‌ها", cls="tall"))
    o.append(fig("diag/deploy.png", "چرخهٔ یکپارچه‌سازی و استقرار پیوسته", cls="w90"))

    # ------------------------------------------------------------------ 3-9
    o.append(h2("3-9", "آزمون و تضمین کیفیت"))
    o.append(p("سه سطح بررسی خودکار در پروژه وجود دارد که پیش از هر ادغام کد اجرا می‌شوند: بررسی نوع با " + code("tsc --noEmit") + " در حالت سخت‌گیرانه، تحلیل ایستا با ESLint (شامل قواعد React Compiler برای جلوگیری از رندرهای آبشاری) و ۷۶ آزمون واحد Vitest در ۱۸ فایل. جدول " + fa(3) + "-" + fa(4) + " پوشش آزمون‌ها را به تفکیک حوزه نشان می‌دهد. زمان اجرای کل مجموعه کمتر از ۶ ثانیه است که اجرای مکرر آن را در حین توسعه عملی می‌کند.", "first"))
    o.append(table("پوشش آزمون‌های واحد به تفکیک حوزه", ["فایل آزمون", "حوزه", "نمونهٔ موارد آزمون"], [
        [en("auth.test.ts, auth-validation"), "احراز هویت", "هش و بررسی گذرواژه، امضا و اعتبار JWT، قواعد ایمیل/گذرواژه"],
        [en("account-tokens.test.ts"), "بازیابی حساب", "تولید، هش و انقضای توکن یک‌بارمصرف"],
        [en("admin/trainer/member-validation"), "اعتبارسنجی ورودی", "طرح اشتراک، پروفایل مربی، اندازه‌های بدنی، مقادیر مرزی"],
        [en("plan-validation.test.ts"), "برنامه‌ها", "ساختار روز/حرکت، ست و تکرار، کالری"],
        [en("scheduling.test.ts"), "زمان‌بندی", "تداخل بازه‌ها، قرار گرفتن در بازهٔ در دسترس"],
        [en("finance-report.test.ts"), "مالی", "جمع به تفکیک ارز، فقط پرداخت موفق، کسر بازپرداخت، درآمد ماهانه"],
        [en("streak.test.ts, engagement.test.ts"), "درگیری کاربر", "استریک با منطقهٔ زمانی تهران، شاگردان کم‌فعال، گفت‌وگوهای بی‌پاسخ"],
        [en("fa-utils.test.ts"), "بومی‌سازی", "تبدیل جلالی، ارقام فارسی، قالب پول"],
        [en("payment-provider.test.ts"), "پرداخت", "توکن نمایشی، امضای HMAC، رد امضای نامعتبر"],
        [en("csv-export.test.ts, schema.test.ts, hosted-sql"), "زیرساخت", "گریز کاراکترهای CSV، سازگاری طرحواره، اتصال libSQL"],
    ], widths=["30%", "16%", "54%"]))

    # ================================================================== USER GUIDE
    o.append(h2("3-10", "راهنمای استفاده: ورود به سامانه"))
    o.append(p("بخش دوم این فصل، راهنمای گام‌به‌گام کار با سامانه است. تصاویر از نسخهٔ فارسی سامانه با داده‌های نمونه گرفته شده‌اند. ابتدا مراحل مشترک (ثبت‌نام و ورود) و سپس راهنمای اختصاصی هر نقش ارائه می‌شود.", "first"))
    o.append(h3("3-10-1", "صفحهٔ اصلی"))
    o.append(p("با ورود به نشانی سامانه، صفحهٔ اصلی (شکل " + fa(3) + "-" + fa(19) + ") نمایش داده می‌شود. نوار بالای صفحه شامل پیوندهای بخش‌ها، سوییچ زبان (فارسی/English)، دکمهٔ روز/شب، «ورود» و «شروع کن» است. در ادامهٔ صفحه، معرفی قابلیت‌ها برای هر سه نقش، شمارنده‌های زنده و بخش پرسش‌های متداول قرار دارد.", "first"))
    o.append(fig("shots/landing.png", "صفحهٔ اصلی سامانه", shot=True))
    o.append(h3("3-10-2", "ثبت‌نام"))
    o.append(p("برای ساخت حساب، روی «شروع کن» یا «حساب کاربری بساز» کلیک کنید (شکل " + fa(3) + "-" + fa(20) + ") و مراحل زیر را دنبال کنید:", "first"))
    o.append(ol([
        "نام کامل خود را وارد کنید.",
        "نشانی ایمیل معتبر وارد کنید؛ این نشانی برای ورود و بازیابی گذرواژه استفاده می‌شود.",
        "گذرواژه‌ای با حداقل ۸ کاراکتر شامل حرف و عدد انتخاب کنید.",
        "نقش خود را انتخاب کنید: «عضو» برای ورزشکاران و «مربی» برای مربیان. حساب مدیر باشگاه با انتخاب «مدیر» ساخته می‌شود و پس از ورود می‌توانید باشگاه خود را بسازید.",
        "روی «حساب من را بساز» کلیک کنید. پس از ثبت موفق، به‌طور خودکار وارد پنل نقش خود می‌شوید.",
    ]))
    o.append(fig("shots/register.png", "فرم ثبت‌نام با انتخاب نقش", shot=True))
    o.append(h3("3-10-3", "ورود و بازیابی گذرواژه"))
    o.append(p("در صفحهٔ ورود (شکل " + fa(3) + "-" + fa(21) + ") ایمیل و گذرواژه را وارد کنید. با فعال کردن «مرا به خاطر بسپار» نشست شما ۷ روز (به‌جای ۲۴ ساعت) معتبر می‌ماند. اگر گذرواژه را فراموش کرده‌اید، پیوند «فراموشی گذرواژه» شما را به فرمی می‌برد که با وارد کردن ایمیل، پیوند بازنشانی ۳۰ دقیقه‌ای دریافت می‌کنید. دکمه‌های زبان و روز/شب در گوشهٔ بالای این صفحات نیز در دسترس‌اند.", "first"))
    o.append(two_up("shots/login.png", "shots/forgot.png", "صفحهٔ ورود (راست) و فراموشی گذرواژه (چپ)"))

    # ------------------------------------------------------------------ ADMIN GUIDE
    o.append(h2("3-11", "راهنمای مدیر باشگاه"))
    o.append(h3("3-11-1", "فضاهای کاری و ساخت باشگاه"))
    o.append(p("پس از ورود، مدیر فهرست باشگاه‌های خود را می‌بیند (شکل " + fa(3) + "-" + fa(22) + "). برای ساخت باشگاه جدید، در فرم سمت راست نام، شهر و توضیح کوتاه را وارد و «ساخت باشگاه» را بزنید. هر باشگاه یک «فضای کاری» مستقل با داشبورد، اعضا، مربیان و مالی جداگانه است و مدیر می‌تواند چند باشگاه را اداره کند. با کلیک روی کارت هر باشگاه وارد فضای کاری آن می‌شوید.", "first"))
    o.append(fig("shots/admin-gyms.png", "فهرست فضاهای کاری مدیر و فرم ساخت باشگاه", shot=True))
    o.append(h3("3-11-2", "داشبورد"))
    o.append(p("داشبورد (شکل " + fa(3) + "-" + fa(23) + ") در یک نگاه وضعیت باشگاه را نشان می‌دهد: تعداد اعضا و مربیان فعال، اشتراک‌های فعال و در آستانهٔ انقضای این هفته، درآمد خالص، نمودار درآمد شش ماه گذشته، درخواست‌های در انتظار بررسی و «اقدامات سریع». نوار کناری سمت راست دسترسی به بخش‌های اعضا، مربی‌ها، برنامه‌ها، اشتراک‌ها، پرداخت‌ها و تنظیمات را فراهم می‌کند و در پایین آن سوییچ زبان، دکمهٔ روز/شب و خروج قرار دارد.", "first"))
    o.append(fig("shots/admin-overview.png", "داشبورد مدیر باشگاه", shot=True, cls="shot-tall", style="max-height:17cm"))
    o.append(h3("3-11-3", "مدیریت اعضا"))
    o.append(p("در بخش «اعضا» (شکل " + fa(3) + "-" + fa(24) + ") فهرست همهٔ درخواست‌ها و عضویت‌ها با زبانه‌های وضعیت (همه، در انتظار، تأییدشده، تعلیق) و جست‌وجو نمایش داده می‌شود. برای هر درخواست می‌توانید «تأیید» یا «رد» را بزنید، یا با انتخاب چند ردیف، عملیات گروهی انجام دهید. دکمهٔ «افزودن عضو» برای ثبت‌نام حضوری است: ایمیل عضو را وارد کنید تا اگر حساب داشته باشد مستقیماً عضو شود و در غیر این صورت دعوت‌نامه دریافت کند. ستون «اشتراک» وضعیت فعلی اشتراک هر عضو را نشان می‌دهد.", "first"))
    o.append(fig("shots/admin-members.png", "مدیریت اعضا و درخواست‌های عضویت", shot=True))
    o.append(h3("3-11-4", "مدیریت مربیان"))
    o.append(p("بخش «مربی‌ها» (شکل " + fa(3) + "-" + fa(25) + ") مشابه اعضا عمل می‌کند: درخواست‌های همکاری مربیان را ببینید، پروفایل و تخصص‌هایشان را بررسی و تأیید یا رد کنید. برای هر مربی تعداد شاگردان فعال در این باشگاه نمایش داده می‌شود. مربی تأییدشده در صفحهٔ عمومی باشگاه به اعضا معرفی می‌شود.", "first"))
    o.append(fig("shots/admin-trainers.png", "مدیریت مربیان باشگاه", shot=True))
    o.append(h3("3-11-5", "طرح‌های اشتراک"))
    o.append(p("در بخش «برنامه‌ها» (شکل " + fa(3) + "-" + fa(26) + ") طرح‌های اشتراک باشگاه را تعریف کنید. برای هر طرح نام، مدت (روز)، قیمت، ارز و مخاطب (عضو یا مربی) را مشخص کنید. طرح‌ها را می‌توان غیرفعال کرد بدون آنکه اشتراک‌های خریداری‌شدهٔ قبلی آسیب ببینند، زیرا قیمت پرداخت‌شده در خود اشتراک ذخیره می‌شود.", "first"))
    o.append(fig("shots/admin-plans.png", "تعریف و مدیریت طرح‌های اشتراک", shot=True))
    o.append(h3("3-11-6", "اشتراک‌ها"))
    o.append(p("بخش «اشتراک‌ها» (شکل " + fa(3) + "-" + fa(27) + ") همهٔ اشتراک‌های اعضا را با وضعیت، تاریخ شروع و پایان و روزهای باقی‌مانده فهرست می‌کند. می‌توانید اشتراکی را دستی فعال، تمدید یا لغو کنید (مثلاً برای پرداخت نقدی) و با عملیات گروهی چند اشتراک را هم‌زمان تغییر دهید. اشتراک‌های در آستانهٔ انقضا با رنگ هشدار مشخص شده‌اند.", "first"))
    o.append(fig("shots/admin-subscriptions.png", "فهرست و مدیریت اشتراک‌ها", shot=True))
    o.append(h3("3-11-7", "پرداخت‌ها و گزارش مالی"))
    o.append(p("صفحهٔ «پرداخت‌ها و حسابرسی» (شکل " + fa(3) + "-" + fa(28) + ") سه بخش دارد: خلاصهٔ مالی به تفکیک ارز (درآمد ناخالص، بازپرداخت، خالص، تعداد تمدیدها)، فهرست پرداخت‌ها با امکان بازپرداخت پرداخت‌های موفق، و خط زمانی رخدادهای اداری (چه کسی، چه زمانی، چه کاری کرد). دکمهٔ «خروجی CSV» فهرست اعضا، مربیان، اشتراک‌ها یا پرداخت‌ها را برای استفاده در اکسل یا نرم‌افزار حسابداری دانلود می‌کند. توجه کنید که فقط پرداخت‌های موفق در درآمد شمرده می‌شوند و پرداخت‌های ناموفق یا در انتظار هرگز جمع را افزایش نمی‌دهند.", "first"))
    o.append(fig("shots/admin-payments.png", "پرداخت‌ها، گزارش مالی و خط زمانی حسابرسی", shot=True, cls="shot-tall", style="max-height:17cm"))
    o.append(h3("3-11-8", "تنظیمات باشگاه"))
    o.append(p("در «تنظیمات باشگاه» (شکل " + fa(3) + "-" + fa(29) + ") می‌توانید نام، شهر، نشانی، توضیحات، ساعت کاری و وضعیت باشگاه (فعال/غیرفعال) را ویرایش کنید. غیرفعال کردن باشگاه، آن را از صفحهٔ کشف باشگاه‌ها پنهان می‌کند اما داده‌ها حفظ می‌شود.", "first"))
    o.append(fig("shots/admin-settings.png", "تنظیمات باشگاه", shot=True, cls="shot-tall", style="max-height:16cm"))

    # ------------------------------------------------------------------ TRAINER GUIDE
    o.append(h2("3-12", "راهنمای مربی"))
    o.append(h3("3-12-1", "داشبورد مربی"))
    o.append(p("داشبورد مربی (شکل " + fa(3) + "-" + fa(30) + ") تعداد شاگردان فعال، درخواست‌های در انتظار، جلسات امروز و برنامه‌های منتشرشده را نشان می‌دهد و شاگردانی را که بیش از یک هفته فعالیت نداشته‌اند برجسته می‌کند تا با یک کلیک برایشان یادآور بفرستید. نوار کناری (نارنجی، برای تمایز از پنل مدیر) شامل شاگردان، پیشرفت مراجعان، باشگاه‌ها، برنامه‌های تمرینی و غذایی، برنامهٔ زمانی و پروفایل است.", "first"))
    o.append(fig("shots/trainer-overview.png", "داشبورد مربی", shot=True))
    o.append(h3("3-12-2", "تکمیل پروفایل و پیوستن به باشگاه"))
    o.append(p("پیش از هر چیز، پروفایل حرفه‌ای خود را در «پروفایل مربی» (شکل " + fa(3) + "-" + fa(31) + ") تکمیل کنید: بیوگرافی، تخصص‌ها، سال‌های تجربه و مدارک. این اطلاعات در صفحهٔ عمومی مربیان به اعضا نمایش داده می‌شود. سپس در «باشگاه‌های من» (شکل " + fa(3) + "-" + fa(32) + ") باشگاه‌های فعال را ببینید و برای همکاری «درخواست» بدهید؛ پس از تأیید مدیر، شاگردان آن باشگاه می‌توانند شما را انتخاب کنند. می‌توانید هم‌زمان با چند باشگاه همکاری کنید.", "first"))
    o.append(fig("shots/trainer-profile.png", "پروفایل حرفه‌ای مربی", shot=True, cls="shot-tall", style="max-height:17cm"))
    o.append(fig("shots/trainer-gyms.png", "باشگاه‌های مربی و درخواست همکاری", shot=True))
    o.append(h3("3-12-3", "مدیریت شاگردان"))
    o.append(p("در «شاگردان» (شکل " + fa(3) + "-" + fa(33) + ") درخواست‌های مربی‌گری اعضا را بپذیرید یا رد کنید و فهرست شاگردان فعال را ببینید. برای هر شاگرد، تعداد تمرین‌ها، برنامه‌های غذایی و جلسات و آخرین فعالیت نمایش داده می‌شود. از همین‌جا می‌توانید جلسه زمان‌بندی کنید، رابطه را موقتاً متوقف یا پایان دهید. دکمهٔ «دعوت شاگرد» به شما امکان می‌دهد با ایمیل، عضوی را مستقیماً دعوت کنید.", "first"))
    o.append(fig("shots/trainer-students.png", "مدیریت شاگردان", shot=True))
    o.append(h3("3-12-4", "ساخت برنامهٔ تمرینی"))
    o.append(p("در «برنامه‌های تمرینی» (شکل " + fa(3) + "-" + fa(34) + ") روی «برنامهٔ جدید» کلیک کنید و مراحل زیر را دنبال کنید:", "first"))
    o.append(ol([
        "عنوان و توضیح برنامه و هدف آن (قدرت، کاهش وزن، آمادگی عمومی و…) را وارد کنید.",
        "روزهای تمرین را اضافه کنید (مثلاً «روز اول – بالاتنه»).",
        "برای هر روز، حرکات را از کتابخانه انتخاب یا دستی وارد کنید و ست، تکرار، وزنهٔ پیشنهادی و زمان استراحت را مشخص کنید.",
        "برنامه را ذخیره کنید؛ در حالت «پیش‌نویس» فقط شما آن را می‌بینید.",
        "با «انتشار»، برنامه آمادهٔ تخصیص می‌شود. برای شخصی‌سازی برای هر شاگرد، از «کپی» استفاده کنید تا الگو دست‌نخورده بماند.",
        "در «تخصیص»، شاگرد و تاریخ شروع را انتخاب کنید. شاگرد اعلان دریافت می‌کند و برنامه در پنل او ظاهر می‌شود.",
    ]))
    o.append(fig("shots/trainer-workouts.png", "فهرست برنامه‌های تمرینی مربی", shot=True))
    o.append(h3("3-12-5", "ساخت برنامهٔ غذایی"))
    o.append(p("«برنامه‌های غذایی» (شکل " + fa(3) + "-" + fa(35) + ") ساختار مشابهی دارد: روزها، وعده‌ها (صبحانه، ناهار، میان‌وعده و…) و مواد غذایی هر وعده با مقدار و کالری. هدف کالری روزانه در بالای برنامه تنظیم می‌شود و جمع کالری هر روز به‌طور خودکار محاسبه می‌گردد. تخصیص و کپی مانند برنامهٔ تمرینی است.", "first"))
    o.append(fig("shots/trainer-nutrition.png", "برنامه‌های غذایی مربی", shot=True))
    o.append(h3("3-12-6", "برنامهٔ زمانی و جلسات"))
    o.append(p("در «برنامهٔ زمانی» (شکل " + fa(3) + "-" + fa(36) + ") ابتدا بازه‌های در دسترس هفتگی خود را تعریف کنید (روز هفته، ساعت شروع و پایان). سپس جلسات رزروشده را در تقویم ببینید، جلسهٔ جدید برای شاگرد ثبت کنید (سامانه تداخل را بررسی می‌کند) و وضعیت جلسات گذشته را به «انجام‌شده» یا «غیبت» تغییر دهید. یادآوری جلسه به‌طور خودکار برای شاگرد ارسال می‌شود.", "first"))
    o.append(fig("shots/trainer-schedule.png", "برنامهٔ زمانی مربی: بازه‌های در دسترس و تقویم جلسات", shot=True, cls="shot-tall", style="max-height:17cm"))
    o.append(h3("3-12-7", "پیگیری پیشرفت و بازخورد"))
    o.append(p("«پیشرفت مراجعان» (شکل " + fa(3) + "-" + fa(37) + ") برای هر شاگرد خلاصهٔ تمرین‌های ثبت‌شده، شدت درک‌شده، اندازه‌های بدنی و عکس‌های پیشرفت را نشان می‌دهد. با کلیک روی هر شاگرد، جزئیات هر جلسهٔ تمرین (ست‌ها، تکرارها و وزنه‌های واقعی) را می‌بینید و می‌توانید زیر آن بازخورد بنویسید؛ شاگرد اعلان می‌گیرد و می‌تواند پاسخ دهد. گفت‌وگوهایی که بیش از ۲۴ ساعت بی‌پاسخ مانده‌اند در داشبورد یادآوری می‌شوند.", "first"))
    o.append(fig("shots/trainer-progress.png", "پیشرفت مراجعان", shot=True))

    # ------------------------------------------------------------------ MEMBER GUIDE
    o.append(h2("3-13", "راهنمای عضو (ورزشکار)"))
    o.append(h3("3-13-1", "داشبورد عضو"))
    o.append(p("داشبورد عضو (شکل " + fa(3) + "-" + fa(38) + ") با سلام و تاریخ شمسی امروز آغاز می‌شود و تمرین امروز، استریک (روزهای پیاپی تمرین)، اشتراک فعال و روزهای باقی‌ماندهٔ آن، اهداف و آخرین اعلان‌ها را نشان می‌دهد. نوار کناری (سبز لیمویی) شامل کشف باشگاه‌ها، یافتن مربی‌ها، تمرین‌ها، تغذیه، برنامهٔ زمانی، پیشرفت من، اشتراک‌ها، اعلان‌ها و پروفایل است.", "first"))
    o.append(fig("shots/member-overview.png", "داشبورد عضو", shot=True, cls="shot-tall", style="max-height:17cm"))
    o.append(h3("3-13-2", "پیوستن به باشگاه و خرید اشتراک"))
    o.append(p("در «کشف باشگاه‌ها» (شکل " + fa(3) + "-" + fa(39) + ") باشگاه‌های فعال را با شهر، امتیاز، تعداد مربیان و طرح‌ها ببینید. با ورود به صفحهٔ باشگاه:", "first"))
    o.append(ol([
        "«درخواست عضویت» را بزنید و منتظر تأیید مدیر بمانید (اعلان دریافت می‌کنید).",
        "پس از تأیید، یکی از طرح‌های اشتراک را انتخاب و «خرید» را بزنید.",
        "به صفحهٔ پرداخت هدایت می‌شوید؛ پس از پرداخت موفق، اشتراک بلافاصله فعال می‌شود.",
        "در «اشتراک‌ها» (شکل " + fa(3) + "-" + fa(40) + ") می‌توانید رسید را دانلود کنید، تمدید خودکار را روشن/خاموش کنید، اشتراک را موقتاً متوقف (مثلاً در سفر) و بعداً ازسرگیری کنید، یا آن را لغو کنید.",
    ]))
    o.append(fig("shots/member-gyms.png", "کشف باشگاه‌ها", shot=True))
    o.append(fig("shots/member-subscriptions.png", "اشتراک‌های عضو با امکان توقف، ازسرگیری، لغو و دریافت رسید", shot=True))
    o.append(h3("3-13-3", "انتخاب مربی"))
    o.append(p("در «یافتن مربی‌ها» (شکل " + fa(3) + "-" + fa(41) + ") همهٔ مربیان پلتفرم – نه فقط مربیان باشگاه شما – با تخصص، تجربه و امتیاز فهرست شده‌اند. پروفایل مربی را ببینید و «درخواست مربی‌گری» بدهید. پس از پذیرش، مربی می‌تواند برایتان برنامه بنویسد و جلسه بگذارد. می‌توانید هم‌زمان با بیش از یک مربی (مثلاً یکی برای تمرین و یکی برای تغذیه) کار کنید.", "first"))
    o.append(fig("shots/member-trainers.png", "یافتن مربی در سراسر پلتفرم", shot=True))
    o.append(h3("3-13-4", "اجرای تمرین"))
    o.append(p("«تمرین‌ها» (شکل " + fa(3) + "-" + fa(42) + ") برنامهٔ فعال شما را با روزهای آن نشان می‌دهد. برای اجرای یک روز تمرینی:", "first"))
    o.append(ol([
        "روی «شروع تمرین» روز مورد نظر کلیک کنید.",
        "برای هر حرکت، پس از انجام هر ست، تعداد تکرار و وزنهٔ واقعی را وارد کنید. تایمر استراحت به‌طور خودکار با زمان تعیین‌شده توسط مربی شروع می‌شود.",
        "در پایان، شدت درک‌شدهٔ کل جلسه (۱ تا ۱۰) و یادداشت اختیاری را ثبت کنید.",
        "«پایان تمرین» را بزنید. جلسه در «سوابق تمرین» ثبت، استریک شما به‌روز و مربی مطلع می‌شود.",
    ]))
    o.append(fig("shots/member-workouts.png", "برنامهٔ تمرینی عضو و سوابق اجرا", shot=True))
    o.append(h3("3-13-5", "تغذیه"))
    o.append(p("در «تغذیه» (شکل " + fa(3) + "-" + fa(43) + ") برنامهٔ غذایی تخصیص‌یافته را ببینید و هر وعده را پس از مصرف «تیک» بزنید یا مقدار واقعی را ثبت کنید. جمع کالری روز در مقایسه با هدف نمایش داده می‌شود و سابقهٔ روزهای گذشته برای مربی قابل مشاهده است.", "first"))
    o.append(fig("shots/member-nutrition.png", "برنامهٔ غذایی و ثبت وعده‌ها", shot=True))
    o.append(h3("3-13-6", "برنامهٔ زمانی"))
    o.append(p("«برنامهٔ زمانی» (شکل " + fa(3) + "-" + fa(44) + ") جلسات حضوری شما با مربی را در تقویم هفتگی نشان می‌دهد. برای رزرو جلسهٔ جدید، مربی و یکی از بازه‌های آزاد او را انتخاب کنید؛ اگر بازه اشغال شده باشد سامانه اطلاع می‌دهد. یادآوری جلسه طبق تنظیم شما (پیش‌فرض ۲۴ ساعت قبل) ارسال می‌شود.", "first"))
    o.append(fig("shots/member-schedule.png", "برنامهٔ زمانی و رزرو جلسه", shot=True))
    o.append(h3("3-13-7", "پیگیری پیشرفت"))
    o.append(p("«پیشرفت من» (شکل " + fa(3) + "-" + fa(45) + ") سه ابزار دارد: ثبت اندازه‌های بدنی (وزن، دور کمر، درصد چربی و…) با نمودار روند؛ آپلود عکس پیشرفت با تاریخ برای مقایسهٔ بصری؛ و تعریف اهداف (مثلاً «رسیدن به وزن ۷۵ کیلوگرم تا پایان تابستان») با نمایش درصد پیشرفت. همهٔ این داده‌ها متعلق به حساب شماست و با تغییر باشگاه یا مربی حفظ می‌شود.", "first"))
    o.append(fig("shots/member-progress.png", "پیگیری پیشرفت: اندازه‌ها، نمودار، عکس‌ها و اهداف", shot=True, cls="shot-tall", style="max-height:17cm"))
    o.append(h3("3-13-8", "اعلان‌ها و پروفایل"))
    o.append(p("«اعلان‌ها» (شکل " + fa(3) + "-" + fa(46) + ") همهٔ رویدادها – تخصیص برنامه، بازخورد مربی، یادآوری جلسه، هشدار انقضای اشتراک – را فهرست می‌کند و تعداد خوانده‌نشده‌ها روی آیکن زنگ نمایش داده می‌شود. در بخش تنظیمات همین صفحه می‌توانید زمان یادآوری جلسه (ساعت)، هشدار انقضا (روز) و یادآور بی‌فعالیتی را تنظیم یا خاموش کنید. در «پروفایل» (شکل " + fa(3) + "-" + fa(47) + ") اطلاعات شخصی، تغییر گذرواژه و فهرست دستگاه‌های وارد‌شده با امکان «خروج از سایر دستگاه‌ها» قرار دارد.", "first"))
    o.append(fig("shots/member-notifications.png", "اعلان‌ها و تنظیمات یادآوری", shot=True))
    o.append(fig("shots/member-profile.png", "پروفایل عضو، تغییر گذرواژه و نشست‌های فعال", shot=True, cls="shot-tall", style="max-height:14.5cm"))
    o.append(h3("3-13-9", "استفاده در گوشی همراه"))
    o.append(p("همهٔ صفحات بالا در گوشی همراه نیز قابل استفاده‌اند. در عرض‌های کوچک، منو به دکمهٔ همبرگری در سرصفحه منتقل می‌شود و با لمس آن، کشوی ناوبری با همهٔ بخش‌ها، سوییچ زبان، دکمهٔ روز/شب و خروج باز می‌شود (شکل " + fa(3) + "-" + fa(15) + "). نوار زبانه‌های زیر سرصفحه نیز دسترسی سریع به بخش‌های پرکاربرد را فراهم می‌کند. فرم‌ها با اندازهٔ قلم مناسب طراحی شده‌اند تا مرورگر گوشی هنگام تمرکز روی فیلد، صفحه را بزرگ‌نمایی نکند.", "first"))
    o.append(end_chapter())
    return "\n".join(o)
